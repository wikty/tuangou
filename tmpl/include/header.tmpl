<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>{{ site_name }}</title>

    <script type="text/javascript" src="{{ static_url }}/jquery-2.0.0.min.js"></script>
    <script type="text/javascript" src="{{ static_url }}/bootstrap.min.js"></script>
    <script type="text/javascript" src="{{ static_url }}/jquery-ui.js"></script>
    <script type="text/javascript" src="{{ static_url }}/holder.js"></script>
    <script type="text/javascript" src="{{ static_url }}/main.js"></script>
    

    <link href="{{ static_url }}/bootstrap.min.css" rel="stylesheet">
    <link href="{{ static_url }}/bootstrap-responsive.min.css" rel="stylesheet" />
    <link type="text/css" href="{{ static_url }}/jquery-ui.css" />
    <link type="text/css" href="{{ static_url }}/main.css" />
    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="{{ static_url }}/html5shiv.js"></script>
    <![endif]-->
  </head>
  <body>
  	    <!-- Part 1: Wrap all page content here -->
    <div id="wrap">
      <div id="pageheader">
        
      </div>
      <div class='container-fluid'>
